{
  "scripts": {
    "postdeploy": "sudo heroku config:set TEST_CONFIG=myvalue"
  },
  "environments": {
    "review": {
      "scripts": {
        "postdeploy": "chmod a+x ./postdeployReviewapp.sh"
      },
      "addons": [
        "heroku-postgresql:hobby-dev"
      ]
    }
  }
}